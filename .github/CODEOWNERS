# Only the people listed here can give the approval that unblocks a merge.
# Team members are collaborators (so they can push branches and run CI) but
# their approvals do NOT count — otherwise they could green-light each
# other's pages and skip review entirely.
#
# Keep this in sync with "admins" in teams.json. Any single listed reviewer
# is enough. Add your TAs / execs before the rush so approvals aren't
# bottlenecked on one person.

*  @Slimebro1231
